// ChatKit — Group chat with AI agents for Teale
//
// Every conversation (DM or group) has an AI agent as a participant.
// Messages sync in real-time via Supabase Realtime.
// Tools (calendar, email, etc.) can be connected per-user, per-conversation.
// Invitations and referrals drive organic growth with credit rewards.

// Re-export public types for convenience
@_exported import struct Foundation.UUID
@_exported import struct Foundation.Date
