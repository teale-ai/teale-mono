# 2026-04-12

## Session: local rebuild for WAN testing

- Renamed the workspace branch to `taylorhou/update-from-main` per Conductor instructions.
- Fetched `origin/main` and confirmed the workspace was already up to date with commit `5916728`.
- Ran `swift build` successfully on this Mac. Build completed with warnings only.
- Ran `./bundle.sh`, which built a Release macOS app via `xcodebuild` and produced `.build/Teale.app`.
- Confirmed the bundle included `mlx-swift_Cmlx.bundle` and other SwiftPM resource bundles.
- Launched `.build/Teale.app` locally after bundling.
- Verified the launched app is listening on `127.0.0.1:11435` for the local API.
- Noted that the bundle is ad-hoc signed (`Signature=adhoc`), so restricted entitlements like multicast are not attached in this local build.

## Session: LAN cluster discovery follow-up

- Investigated a report that two Macs with LAN Cluster enabled did not discover each other.
- Confirmed from unified logs that this app's Bonjour browser became ready, discovered only a self endpoint, and then cancelled after the default 10 second scan window.
- Confirmed later mDNS traffic from another device arrived after the browser had already stopped, which explains missed discovery when peers come online slightly later.
- Changed `ClusterManager.scanForPeers` to browse indefinitely by default while LAN Cluster is enabled; timed auto-stop now only happens when an explicit duration is passed.
- Updated the Cluster UI copy to reflect continuous scanning behavior.
- Rebuilt with `swift build`, rebuilt the Release app via `./bundle.sh`, and relaunched `.build/Teale.app`.
- Remaining caveat: this Mac still has no local signing identity, so the app remains ad-hoc signed for local runs.
