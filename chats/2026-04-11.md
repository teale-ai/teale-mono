# 2026-04-11 — Branch Cleanup, Merge, and CLAUDE.md Restructuring

## What Was Done

### Branch Merges
Merged all 7 unmerged remote branches into `main`:

- **4 stale branches** (already merged via PRs, just deleted):
  - `taylorhou/initial-setup`, `taylorhou/repo-summary`, `taylorhou/solair-summary`, `taylorhou/stripe-credits`, `taylorhou/session-setup`

- **3 branches with new content** (merged with conflict resolution):
  - `taylorhou/download-progress-ui` — ChatKit module, model picker, download progress UI
  - `taylorhou/wan-relay-ui-fixes` — WireGuard/Noise transport replacing QUIC
  - `taylorhou/2026-04-06-sync` — WalletKit (Solana/USDC), localization (5 languages), Exo gateway, LAN fixes, "Keep Mac Awake"

### Conflict Resolution
14 files had merge conflicts in the large sync branch merge. Key decisions:
- Kept HEAD's WireGuard transport (QUICTransport.swift deleted)
- Kept HEAD's AuthManager dedup fix from PR #5
- Kept sync branch's localization (`loc()` function), non-fatal relay connect pattern, Solana wallet UI
- Combined both sides' features (TealeSDK + WalletKit, ChatKit + localization, etc.)
- Preserved correct relay URL: `wss://teale-relay.fly.dev/ws`

### Build Fixes
Three issues found after merge:
1. `AppState.swift`: covariant `Self` in stored property initializer — used literal key string
2. `SettingsView.swift`: missing `@State` vars (`wanRelayURL`, `maxStorage`) and `wanState` scope issue
3. `CompanionAppState.swift`: `@MainActor` needed on `init()` for `ModelManagerService` isolation

### CLAUDE.md Restructuring
- Created `TEALE.md` with all project phases (kept exactly as written), architecture, modules, dependencies
- Rewrote `CLAUDE.md` as codebase best practices: references TEALE.md, branch discipline rules, session logging, code quality guidelines

## Build Status
`swift build` — **clean, zero errors**

## Final State
- All remote branches deleted (only `main` remains)
- Everything pushed to `origin/main`
